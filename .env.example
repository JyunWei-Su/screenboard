# 複製為 .env 後填寫。本檔可提交 Git；.env 會被忽略。
# 請使用 Cloudflare API Token，不要使用 Global API Key。
# 【Token 1：只在本機執行 bash scripts/deploy.sh 時使用；不會上傳到 Worker】
# 建立方式：Cloudflare Dashboard → My Profile → API Tokens → Create Token → Custom token。
# 權限範圍：Account resources 僅選本專案的 CF_ACCOUNT_ID；Zone resources 僅選本專案根網域。
# Account 權限（全部選 Edit）：D1、Workers R2 Storage、Queues、Workers Scripts、Cloudflare Pages。
# Zone 權限：DNS、Workers Routes 均選 Edit；Zone 選 Read（用於自動取得根網域名稱）。
# 用途：建立 D1/R2/Queue、部署 API Worker 與 Pages、寫入 Worker Secrets、綁定 API_DOMAIN。
CLOUDFLARE_API_TOKEN=
# Cloudflare 帳戶 ID 與主要網域（Zone）ID。
CF_ACCOUNT_ID=
CF_ZONE_ID=

# 已託管於 Cloudflare 的網域。API 網域會自動綁定為 Worker Custom Domain。
# ADMIN_DOMAIN 會由部署腳本自動綁定至 Pages 專案，並建立指向 Pages 的 CNAME DNS 記錄。
API_DOMAIN=api.screenboard.example.com
ADMIN_DOMAIN=screenboard.example.com
# 每台裝置會自動建立 ssh-<device-uuid>.<此 CF_ZONE_ID 對應的根網域>。
# 此格式可由 Cloudflare Universal SSL 覆蓋，不需設定額外的 SSH 子網域。
# 可開啟 Cloudflare Access SSH 的管理者 Email，多位使用逗號分隔。

# Cloudflare 資源名稱；首次部署前可自行改名，部署後請維持不變。
# Worker 在 Cloudflare 後台顯示的名稱；不影響 API_DOMAIN。
WORKER_NAME=api-screenboard-example-com
PAGES_PROJECT_NAME=admin-screenboard-example-com
D1_DATABASE_NAME=screenboard
R2_BUCKET_NAME=screenboard
QUEUE_NAME=screenboard

# 以下為 Worker Secret；每個值都應使用不同的隨機字串，不能提交或外流。
# 管理後台登入 Session JWT 的簽章密鑰。
JWT_SECRET=
# 裝置 Agent JWT 的簽章密鑰，必須與 JWT_SECRET 不同。
DEVICE_JWT_SECRET=
# 僅用於建立第一位管理者的初始授權 Token；建立後建議更換或移除。
BOOTSTRAP_TOKEN=
# 加密資料庫中 TOTP 密鑰的 32-byte base64 值：openssl rand -base64 32
TOTP_ENC_KEY=
# 【Token 2：會以 Worker Secret 上傳；供管理後台於新增/刪除裝置時使用】
# 建立方式：Cloudflare Dashboard → My Profile → API Tokens → Create Token → Custom token。
# 權限範圍：Account resources 僅選本專案的 CF_ACCOUNT_ID；Zone resources 僅選本專案根網域。
# Account 權限（全部選 Edit）：Cloudflare Tunnel、Access: Apps and Policies、
# Access: Organizations, Identity Providers, and Groups。
# Zone 權限：DNS 選 Edit；Zone 選 Read。
# 用途：建立/刪除每台裝置的 Tunnel、SSH CNAME DNS，以及 Cloudflare Access SSH 應用程式與政策；
# 也用於自動建立 Email One-time PIN（OTP）登入方式。
# 不要給此 Token D1、R2、Queues、Workers Scripts 或 Pages 權限。
CF_API_TOKEN=
